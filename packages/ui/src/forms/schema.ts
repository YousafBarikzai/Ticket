/**
 * The form contract now lives in `@itsm/contracts`, so the server can validate a
 * submission with the same code the browser used to collect it. Re-exported here
 * because the design system is where a component author looks for it.
 */
export * from '@itsm/contracts';
